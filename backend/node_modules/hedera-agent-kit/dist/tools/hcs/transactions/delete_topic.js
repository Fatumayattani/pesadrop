"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.delete_topic = void 0;
const sdk_1 = require("@hashgraph/sdk");
const delete_topic = async (topicId, client) => {
    const tx = new sdk_1.TopicDeleteTransaction()
        .setTopicId(topicId)
        .freezeWith(client);
    const txResponse = await tx.execute(client);
    const receipt = await txResponse.getReceipt(client);
    const txStatus = receipt.status;
    if (!txStatus.toString().includes('SUCCESS'))
        throw new Error("Topic creation transaction failed");
    return {
        txHash: txResponse.transactionId.toString(),
        status: txStatus.toString(),
    };
};
exports.delete_topic = delete_topic;
//# sourceMappingURL=delete_topic.js.map