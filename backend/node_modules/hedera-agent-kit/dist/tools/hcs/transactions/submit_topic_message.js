"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.submit_topic_message = void 0;
const sdk_1 = require("@hashgraph/sdk");
const submit_topic_message = async (topicId, message, client) => {
    const tx = await new sdk_1.TopicMessageSubmitTransaction({
        topicId: topicId,
        message: message,
    }).execute(client);
    const receiptQuery = tx.getReceiptQuery();
    const txResponse = await receiptQuery.execute(client);
    const receipt = txResponse.status;
    const hash = receiptQuery.transactionId?.toString();
    return {
        status: receipt.toString(),
        txHash: hash || 'error',
    };
};
exports.submit_topic_message = submit_topic_message;
//# sourceMappingURL=submit_topic_message.js.map