"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.dissociate_token = void 0;
const sdk_1 = require("@hashgraph/sdk");
const dissociate_token = async (tokenId, client) => {
    const tx = await new sdk_1.TokenDissociateTransaction()
        .setAccountId(client.operatorAccountId.toString())
        .setTokenIds([tokenId]);
    const txResponse = await tx.execute(client);
    const receipt = await txResponse.getReceipt(client);
    const txStatus = receipt.status;
    if (!txStatus.toString().includes('SUCCESS'))
        throw new Error("Token dissociation failed");
    return {
        status: txStatus.toString(),
        txHash: txResponse.transactionId.toString(),
    };
};
exports.dissociate_token = dissociate_token;
//# sourceMappingURL=dissociate_token.js.map