"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transfer_token = void 0;
const sdk_1 = require("@hashgraph/sdk");
const transfer_token = async (tokenId, toAccountId, amount, client) => {
    const tx = new sdk_1.TransferTransaction()
        .addTokenTransfer(tokenId, client.operatorAccountId, -amount)
        .addTokenTransfer(tokenId, toAccountId, amount);
    const txResponse = await tx.execute(client);
    const receipt = await txResponse.getReceipt(client);
    const txStatus = receipt.status;
    if (!txStatus.toString().includes('SUCCESS'))
        throw new Error("Token Transfer Transaction failed");
    return {
        status: txStatus.toString(),
        txHash: txResponse.transactionId.toString(),
    };
};
exports.transfer_token = transfer_token;
//# sourceMappingURL=transfer.js.map