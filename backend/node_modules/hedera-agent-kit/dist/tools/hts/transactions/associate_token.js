"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.associate_token = void 0;
const sdk_1 = require("@hashgraph/sdk");
const associate_token = async (tokenId, client) => {
    const tx = await new sdk_1.TokenAssociateTransaction()
        .setAccountId(client.operatorAccountId.toString())
        .setTokenIds([tokenId]);
    const txResponse = await tx.execute(client);
    const receipt = await txResponse.getReceipt(client);
    const txStatus = receipt.status;
    if (!txStatus.toString().includes('SUCCESS'))
        throw new Error("Token Association failed");
    return {
        status: txStatus.toString(),
        txHash: txResponse.transactionId.toString(),
    };
};
exports.associate_token = associate_token;
//# sourceMappingURL=associate_token.js.map