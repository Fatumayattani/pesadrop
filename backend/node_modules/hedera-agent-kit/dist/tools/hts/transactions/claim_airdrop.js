"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.claim_airdrop = void 0;
const sdk_1 = require("@hashgraph/sdk");
const claim_airdrop = async (client, airdropId) => {
    const tx = new sdk_1.TokenClaimAirdropTransaction()
        .addPendingAirdropId(airdropId)
        .freezeWith(client);
    const txResponse = await tx.execute(client);
    const receipt = await txResponse.getReceipt(client);
    const txStatus = receipt.status;
    if (!txStatus.toString().includes('SUCCESS'))
        throw new Error("Token Airdrop Transaction failed");
    return {
        status: txStatus.toString(),
        txHash: txResponse.transactionId.toString(),
    };
};
exports.claim_airdrop = claim_airdrop;
//# sourceMappingURL=claim_airdrop.js.map