"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.reject_token = void 0;
const sdk_1 = require("@hashgraph/sdk");
const reject_token = async (tokenId, client) => {
    const tx = new sdk_1.TokenRejectTransaction()
        .setOwnerId(client.operatorAccountId)
        .addTokenId(tokenId)
        .freezeWith(client);
    const txResponse = await tx.execute(client);
    const receipt = await txResponse.getReceipt(client);
    const txStatus = receipt.status;
    if (!txStatus.toString().includes('SUCCESS'))
        throw new Error("Token Rejection Transaction failed");
    return {
        status: txStatus.toString(),
        txHash: txResponse.transactionId.toString()
    };
};
exports.reject_token = reject_token;
//# sourceMappingURL=reject_token.js.map